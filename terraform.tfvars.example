# Exemple de configuration Terraform
# Copiez ce fichier en terraform.tfvars et remplissez vos valeurs

project_id   = "your-gcp-project-id"
region       = "europe-west1"
default_zone = "europe-west1-b"

# Réseau (utilisez "default" ou votre VPC personnalisé)
network_name = "default"
subnet_name  = "default"

# Instances Workbench à créer
# REMARQUE : Chaque instance peut avoir son propre service account dédié
notebook_instances = {
  "data-scientist-alice" = {
    owner                 = "alice@company.com"
    machine_type          = "e2-standard-2"
    idle_timeout_seconds  = 3600 # 1 heure
    service_account_email = "alice-sa@your-project.iam.gserviceaccount.com" # Service account dédié
  }

  "data-scientist-bob" = {
    owner                 = "bob@company.com"
    machine_type          = "n1-standard-4"
    idle_timeout_seconds  = 7200                                           # 2 heures
    service_account_email = "bob-sa@your-project.iam.gserviceaccount.com" # Service account dédié
  }
}

# Labels pour suivi des coûts
common_labels = {
  team        = "data-science"
  cost_center = "research"
  environment = "dev"
  managed_by  = "terraform"
}